NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
RESEND_API_KEY=
NOTIFICATION_FROM_EMAIL=
# Public base URL of this deployment, e.g. https://tracker.example.com — used to
# build the "View the ticket" link in notification emails. Read server-side
# only (no NEXT_PUBLIC_ prefix). Emails still send (without a link) when this
# is unset.
SITE_URL=
