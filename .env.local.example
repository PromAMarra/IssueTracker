NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
RESEND_API_KEY=
NOTIFICATION_FROM_EMAIL=
# Public base URL of this deployment, e.g. https://tracker.example.com — used to
# build the "View the ticket" link in notification emails. Emails still send
# (without a link) when this is unset.
NEXT_PUBLIC_SITE_URL=
